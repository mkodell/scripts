Uses same token as jira_summary.sh plus a GitHub token that gives repo access

Simply gives you the number of approvals per PR and whether a PR is blocked or not
Looks at the existing watched_keys log for jira_summary.sh to figure out which PRs to check

Doesn't take any parameters